<?php
//

return array
(
	'db_host'         =>  'localhost:3306',
	'db_user'         =>  'root',
	'db_password'        	  =>  '750219',
	'db_name'         =>  'fumushopv2',
	'db_charset'      =>  'utf-8',
	'db_tablepre'     =>  'hicms_',
	'db_usetrans'     =>  true,  # 是否自动进入事务处理
	'db_autocommit'   =>  false, # 是否自动提交
	'db_connect'      =>  0,
	'response_charset'         =>  'utf-8',
	'cache_path'         =>   INDEX_ROOT.'/data',
	'tpldir_path'         =>  APP_ROOT.'/view',
	'objdir_path'         =>  INDEX_ROOT.'/data/view',
	'url_mode'         =>  2,
	'seccodedata'=>array(
		'type'=>1,
	),

    /**
     * HiPHP 内部及 cache 系列函数使用的缓存目录
     * 应用程序必须设置该选项才能使用 cache 功能。
     */
    'runtime_cache_dir'         => INDEX_ROOT.'/data/filecache',

    /**
     * 默认使用的缓存服务
     */
    'runtime_cache_backend'     => 'HiCache_File',

      'runtime_session_start' =>true,


    // {{{ 日志和错误处理

    /**
     * 指示是否允许记录日志
     */
    'log_enabled' => true,

    /**
     * 指示记录哪些优先级的日志（不符合条件的会直接过滤）
     */
    'log_priorities' => 'ERR,DEBUG',

    /**
     * 日志缓存块大小（单位KB）
     *
     * 更小的缓存块可以节约内存，但写入日志的次数更频繁，性能更低。
     */
    'log_cache_chunk_size' => 64,  // 64KB

    /**
     * 保存日志文件的目录
     */
    'log_writer_dir' => INDEX_ROOT.'/data/log',

    /**
     * 日志文件的文件名
     */
    'log_writer_filename' => 'access.log'
);
